fix: 自审轮——修自己五、六轮引入/遗留的四处毛病

- O(n²) 渲染：第五轮 BATCH_RUNNER 过滤改的 map+find 每卡全表扫 → selectableAccounts useMemo（对象级）一次过滤，
  selectableNames 自它派生，两处渲染循环直接 map 对象
- busyRef 渲染覆写窗口：本地置 true 会被「置位后到父级重渲染前」的任意渲染洗回 false（导入/删除/清理三处都有）
  → 拆 localBusyRef，busyRef = 父级 isBusy ∨ localBusyRef（派生式，不再被洗）
- busyAccountsRef 登出残留：resetNotifyWatcherState 同步清 + DashBoard 卸载兜底清（防在途晚到 add 污染下一会话）
- lastConfirmedRef 外部刷新盲区：导入/同步改了配置后，失败回滚会把父级写回外部更新前的旧值
  → inflightPayloadRef 区分本组件乐观回显与外部更新，外部更新时 lastConfirmed 跟进（连点用身份校验防误清）
- ConfigSyncModal PUT 前逐个复查忙碌（源配置拉取是多个 await，过滤快照会过期）