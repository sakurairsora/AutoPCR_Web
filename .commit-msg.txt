refactor: Config 五套保存模板统一为 useConfigSaveFlow（S1）

- 统一：乐观写父级 → 排队保存(enqueueConfigSave) → 成功/失败 toast → 失败回滚父级+显示
- Bool/SingleSelect 经 useConfigState、Int/SingleSearch/Multi/Time/Text 直接迁移，六处共用一个实现
- 各组件差异保留在调用点：Int 的 min/max 显示恢复、Multi 的 string[]→number[] 回滚转换、
  SingleSearch 的 modal 异常分支、Time 的格式校验前置
- 行为语义与迁移前一致；删除五份重复的 mountedRef/valueRef/onUpdateRef 模板

另回滚 B3：选择器保留勾选/搜索状态是用户要的行为（跨开合延续，不复位）