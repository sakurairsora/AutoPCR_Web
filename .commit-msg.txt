fix: 第八轮自查——第七轮 commit 复审两处小修

- useConfigState 死参数 transform 删除：两个调用点（Bool/SingleSelect）均无映射需求，
  留着会诱惑将来误用——误用正好踩中回滚 setState(prev as T) 的类型空间错位
- $account 清理结果 toast 移守卫前：结果告知（成功/警告/错误）与失败 toast 口径对称，
  A 的清理结果无论用户切到哪都应被告知；守卫只挡页面状态写（cleanStatus/refresh）与结果窗